/** The only authorization scheme accepted by the API. */
export const AUTH_SCHEME_BEARER = 'Bearer';

/** Separator between the scheme and the credentials in an Authorization header. */
export const AUTH_HEADER_SEPARATOR = ' ';
