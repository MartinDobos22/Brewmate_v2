# Expo / React Native tooling resolves modules by walking node_modules,
# so the workspace must be hoisted rather than symlinked.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
engine-strict=true
