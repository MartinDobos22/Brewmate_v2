/**
 * Re-exported from the contract, where the bands are defined.
 *
 * The server writes the shop verdict and has to be as honest about how little
 * it knows as the profile screen is, so there is one definition rather than
 * two that could drift apart.
 */
export { resolveConfidenceLevel } from '@brewmate/shared';
