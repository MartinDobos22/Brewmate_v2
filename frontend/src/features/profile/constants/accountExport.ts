/** How the exported document is written and handed over. */
export const EXPORT_MIME_TYPE = 'application/json';

/** The UTI iOS needs to offer the right apps in the share sheet. */
export const EXPORT_UTI = 'public.json';

/** Pretty-printed, because the person opening it is the point. */
export const EXPORT_JSON_INDENT = 2;
