import type { TranslationKey } from '../translationKeys';

/** English copy. No user-visible string may live in a component. */
export const EN_TRANSLATIONS: Record<TranslationKey, string> = {
  appName: 'Brewmate',
  placeholderTitle: 'Backend first',
  placeholderBody: 'The app shell is intentionally empty until the API contract settles.',
};
