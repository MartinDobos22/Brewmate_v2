import type { JSX } from 'react';

import { InsightsScreen } from '../features/history';

/** What this account's brewing adds up to, and the one question it raises. */
export default function InsightsRoute(): JSX.Element {
  return <InsightsScreen />;
}
