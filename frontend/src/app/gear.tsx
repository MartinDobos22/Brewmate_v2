import type { JSX } from 'react';

import { GearScreen } from '../features/profile';

/** What somebody brews with: the kit, the water and the saved sets. */
export default function GearRoute(): JSX.Element {
  return <GearScreen />;
}
