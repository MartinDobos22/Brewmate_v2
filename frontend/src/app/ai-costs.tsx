import type { JSX } from 'react';

import { AiCostScreen } from '../features/profile';

/** What the model calls cost, what is left, and how to take the data away. */
export default function AiCostsRoute(): JSX.Element {
  return <AiCostScreen />;
}
